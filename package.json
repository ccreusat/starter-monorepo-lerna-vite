{
  "name": "root",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ccreusat/starter-monorepo-lerna-vite.git"
  },
  "license": "MIT",
  "scripts": {
    "bootstrap": "pnpm lerna bootstrap",
    "build": "npx lerna run build --ignore=@starter/storybook",
    "build:core": "pnpm lerna run build --scope=@starter/core",
    "build:hooks": "pnpm lerna run build --scope=@starter/hooks",
    "build:icons": "pnpm lerna run build --scope=@starter/icons",
    "build:storybook": "pnpm lerna run build --scope=@starter/storybook",
    "clean": "pnpm lerna clean && rm -rf node_modules",
    "fix": "pnpm lerna run fix",
    "graph": "pnpm nx graph",
    "lint": "pnpm lerna run lint",
    "lint:core": "pnpm lerna run lint --scope=@starter/core",
    "lint:hooks": "pnpm lerna run lint --scope=@starter/hooks",
    "lint:icons": "pnpm lerna run lint --scope=@starter/icons",
    "playground": "pnpm lerna run playground --scope=@starter/storybook",
    "pretty": "pnpm lerna run pretty",
    "storybook": "pnpm lerna run dev --scope=@starter/storybook"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.0.2"
  }
}
