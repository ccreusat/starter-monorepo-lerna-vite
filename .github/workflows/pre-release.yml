name: Pre-Release
on:
  push:
    branches:
      - develop
      - develop*
permissions:
  contents: read #  for checkout
jobs:
  pre-release:
    permissions:
      contents: write #  to be able to publish a GitHub release
      issues: write #  to be able to comment on released issues
      pull-requests: write #  to be able to comment on released pull requests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        name: Install Node
        with:
          node-version: 18

      - uses: pnpm/action-setup@v2
        name: Install pnpm
        id: pnpm-install
        with:
          version: 8.6.6
          run_install: false

      - name: Write NPM_TOKEN
        run: echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - uses: actions/cache@v3
        name: Setup pnpm cache
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - run: pnpm install --no-frozen-lockfile && pnpm build
        name: Install & Build

      - name: Version & Publish
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor}}@users.noreply.github.com"

          if [ "${{ github.ref }}" = "refs/heads/develop-pedago" ]; then
            npx lerna version --conventional-commits --conventional-prerelease --preid develop-pedago --no-changelog --no-git-tag-version --no-private --yes
            git checkout -- packages/**/package.json
            npx lerna publish from-package --preid develop-pedago --pre-dist-tag develop-pedago --yes
          elif [ "${{ github.ref }}" = "refs/heads/develop-b2school" ]; then
            npx lerna version --conventional-commits --conventional-prerelease --preid develop-b2school --no-changelog --no-git-tag-version --no-private --yes
            git checkout -- packages/**/package.json
            npx lerna publish from-package --preid develop-b2school --pre-dist-tag develop-b2school --yes
          else
            echo "your current branch is develop"
            npx lerna version --conventional-commits --conventional-prerelease --preid develop --no-changelog --no-git-tag-version --no-private --yes
            git checkout -- packages/**/package.json
            npx lerna publish from-package --preid develop --pre-dist-tag develop --yes
          fi

        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
